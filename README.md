# price-tracker-extension
# Real-Time Price Tracker (Chrome Extension)

Load this folder as an unpacked Chrome extension (chrome://extensions → Load unpacked).

- Click extension → "Track This Product" to save the current tab's product URL and extracted price.
- Click "View Tracked Products" to see the list of tracked URLs and stored prices.